__pycache__/
*.py[cod]
/build/

# binários compilados da VM (gerados por go build / gcc)
*.exe
vm/pyrovm
vm/pyrovm_go

# 11.23 — incremental compilation cache (content-addressed, disposable)
.cryocache/
